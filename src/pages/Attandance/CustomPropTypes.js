import PropTypes from 'prop-types';

const ViewStyle = PropTypes.shape({
  // Add the relevant style properties for your use case
});

export default {
  ViewStyle,
  // Add other custom PropTypes as needed
};
